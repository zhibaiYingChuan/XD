# 安全策略（Security Policy）

## 报告安全漏洞

道体·玄盾是一款安全产品，我们高度重视安全漏洞的报告。感谢您帮助提升玄盾的安全性。

### 报告流程

1. **请勿在公开 Issue 中报告安全漏洞**
2. 发送邮件至 **spring60@vip.qq.com**，主题标注 `[安全漏洞]`
3. 邮件内容请包含：
   - 漏洞描述与影响范围
   - 复现步骤（最小化复现）
   - 影响的版本号
   - 建议的修复方案（如有）

### 响应时间

| 阶段 | 时间 |
|------|------|
| 确认收到 | 24 小时内 |
| 初步评估 | 3 个工作日内 |
| 修复方案 | 7 个工作日内（P0）/ 30 天（P1） |
| 补丁发布 | 修复完成后 48 小时内 |

### 支持版本

| 版本 | 状态 | 支持截止 |
|------|------|---------|
| 1.3.x | ✅ 当前支持 | 最新版发布后 90 天 |
| 1.2.x | ⚠️ 安全修复 | 1.3.x 发布后 30 天 |
| < 1.2 | ❌ 不支持 | - |

## 安全声明

道体·玄盾为**活性防护**产品，防御能力随在线学习持续增强。文档中引用的"100% 攻击拒绝率"等数据仅反映对**内部基准测试集（213 攻击 + 129 良性样本，37 类攻击探针）**的统计表现，**不承诺对未知攻击的绝对防御**。实际效果取决于部署环境、预热样本质量和在线学习积累。

**安全是过程，不是终点。**

## 安全架构

### 核心安全机制

- **三重检测引擎**：域距离 + 结构/二进制异常 + 4-gram 统计
- **反逆向保护**：Nuitka 编译 + anti_debug + 编译期 key 注入
- **哈希链审计日志**：SHA256 链式防篡改，支持司法取证
- **FALLBACK 降级**：引擎不可达时保护性阻断而非放行
- **路径白名单**：所有文件操作限制在白名单目录内

### 安全不变式（HCSE 验证）

以下 12 条安全不变式经高可信软件工程（HCSE）韧性验证：

1. 引擎未运行时 protect 必须返回 FALLBACK 阻断
2. 引擎不可达时必须审计记录
3. ConfirmModal 并发必须队列化处理
4. 密钥删除必须二次确认
5. 引擎重启必须用户确认
6. 模式同步失败必须返回错误
7. 通知配置同步失败必须返回错误
8. invoke 调用必须超时兜底
9. Tauri Bridge 缺失必须兜底提示
10. 快照恢复必须防并发
11. 密钥存储后必须立即验证
12. 路径操作必须白名单校验

## 漏洞披露政策

- 我们遵循**负责任披露**原则
- 在补丁发布后，我们会在 CHANGELOG 中致谢报告者（除非要求匿名）
- 我们不会对善意报告安全漏洞的行为采取法律行动

## 联系方式

- **安全邮件**：spring60@vip.qq.com
- **PGP Key**：待发布
- **仓库**：https://github.com/zhibaiYingChuan/XD
